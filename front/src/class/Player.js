class Player {
    constructor(tag, x, y, z, precision, instant_speed) {
      this.tag = tag;
      this.x = x;
      this.y = y;
      this.z = z;
      this.precision = precision;
      this.speed = instant_speed;
    }
}
export default Player;